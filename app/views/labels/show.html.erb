<%= link_to "Edit", edit_label_path(@label), class: "edit" %>
<h1><%= @label.name %></h1>
<% unless @label.founder.nil? %>
  <h5>Founded by <%= link_to @label.founder.name, artist_path(@label.founder) %></h5>
<% end %>

<%= render 'shared/songs_table' unless @songs.empty? %>
