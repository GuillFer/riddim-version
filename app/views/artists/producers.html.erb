<% content_for :meta_title, "Producers index - #{DEFAULT_META["meta_website_name"]}" %>

<h1>Producers (<%=@artists.count%>)</h1>

<div class="flex-between">
  <ul id="artists-list">
  <% @artists.each do |a| %>
    <li><%= link_to a.name, artist_path(a) %></li>
  <% end %>
  </ul>

</div>
