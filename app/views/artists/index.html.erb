<h1>Artists (<%=@artists.count%>)</h1>

<div class="flex-between">
  <ul id="artists-list">
  <% @artists.each do |a| %>
    <li><%= link_to a.name, artist_path(a) %> (<%=a.song_artists.count%>)</li>
  <% end %>
  </ul>

</div>

